{
  "name": "oolon-startapp",
  "version": "2.0.0",
  "main": "build/startapp.js",
  "license": "MIT",
  "scripts": {
    "test": "tape -r babel-register 'src/*.test.js' | tap-spec",
    "build": "babel -d build src",
    "watch": "tape-watch -r babel-register 'src/*.test.js' | tap-spec"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-preset-es2015": "^6.22.0",
    "data.task": "^3.1.1",
    "flow-bin": "^0.36.0",
    "sinon": "^1.17.6",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3",
    "tape-watch": "^2.2.4"
  },
  "dependencies": {},
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
